# Not needed for the prototype (it runs on a local SQLite file by default).
# Fill these in and set PULSE_DB_ENGINE=mysql when you're ready to point
# db.py at the real, managed SQL Server / MySQL instance (US-11).

PULSE_DB_ENGINE=sqlite

DB_HOST=
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_PORT=3306
